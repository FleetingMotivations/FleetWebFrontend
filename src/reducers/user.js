import * as All from '../constants/ActionTypes';

const initialState = {
	firstname: null,
	lastname: null,
	title: null ,
	token: "lolololol"
}

export default function user(state = initialState, action) {
	switch(action.type) {
		
		default:
			return state

	}
}