import * as All from '../constants/ActionTypes';

const initialState = {
	userId: null,
	username: null,
	firstname: null,
	lastname: null,
	token: null
}

export default function user(state = initialState, action) {
	switch(action.type) {
		
		default:
			return state

	}
}